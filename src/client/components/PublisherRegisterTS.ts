import { Component, Vue } from 'vue-property-decorator';

const compToBeRegistered: any = {
    
};

@Component({
    components: compToBeRegistered,
})
export class PublisherRegisterTS extends Vue {
    // #region -- referred props and methods by Vue Page
    
    // #endregion


    
}
